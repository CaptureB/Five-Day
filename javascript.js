const key = config.APIKey;
const searchResults = dcoument.getElementById('searchResults');
const searchBtn = document.getElementById('searchBtn');
const searchHistory = document.getElementById('weatherContent');
const fiveDay = document.getElementById('fiveDay');
const cityDetail = document.getElementById('cityDetail');



function getApi() {
    let requestUrl = ""
}